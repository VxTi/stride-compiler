import "system";
import "/test/path/to/file" as test;

fn do_print(content: string, args: ...unknown) {
    printf(content, args);
}

fn main(): void {

    var a: i32 = 10;
    var b: f32 = 3.1, c: f32 = 4.1;

    var k: f32 = a = a * b + c;

    do_print("Calculated value for k: %d", k);

}
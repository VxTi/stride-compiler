
fn main(args: ...string): void {

        printf("Hello, World!");

        math::cos( 0.5 );
        var k: int16 = 10;

        const var immutable: int64;

}

namespace math {

    pub ext fn cos(radians: float32): float32;
    pub ext fn sin(radians: float32): float32;
    pub ext fn tan(radians: float32): float32;
    pub ext fn acos(radians: float32): float32;
    pub ext fn asin(radians: float32): float32;
    pub ext fn atan(radians: float32): float32;
    pub ext fn atan2(y: float32, x: float32): float32;
    pub ext fn cosh(radians: float32): float32;

}

namespace io {

    class inner_test {

        pub fn test() {
            io::log("test");
        }


        pub ext fn exit(status: int32);
        pub ext fn printf(args: ...string);

        while ( true ) {
            for ( var i: int8 = 0; i < 10; i++ ) {

                io::print("test");

            }
        }

    }
    pub fn test(param: int8): int8 {
        return param;
    }
}
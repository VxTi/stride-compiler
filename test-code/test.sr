
fn main(args: ...string): void {

        printf("Hello, World!");

        math::cos( 0.5 );
        var k: i16 = 10;

        const var immutable: int64;

}

namespace math {

    pub ext fn cos(radians: f32): f32;
    pub ext fn sin(radians: f32): f32;
    pub ext fn tan(radians: f32): f32;
    pub ext fn acos(radians: f32): f32;
    pub ext fn asin(radians: f32): f32;
    pub ext fn atan(radians: f32): f32;
    pub ext fn atan2(y: f32, x: f32): f32;
    pub ext fn cosh(radians: f32): f32;

}

namespace io {

    class inner_test {

        pub fn test() {
            io::log("test");
        }


        pub ext fn exit(status: int32);
        pub ext fn printf(args: ...string);

        while ( true ) {
            for ( var i: i8 = 0; i < 10; i++ ) {

                io::print("test");

            }
        }

    }
    pub fn test(param: i8): i8 {
        return param;
    }
}